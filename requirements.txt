# Core UI
PySide6

# Core processing
numpy
opencv-python
scipy
Pillow
matplotlib

# PyTorch is installed separately depending on GPU:
# NVIDIA CUDA: install from https://pytorch.org/get-started/locally/
# AMD / Intel Windows: pip install torch-directml
# CPU fallback: install CPU torch from PyTorch selector

# ONNX / GPU inference
onnxruntime-gpu

# Hugging Face / AI models
transformers
diffusers
accelerate
huggingface_hub
safetensors
hf_xet

# Video / scene tools
scenedetect
imageio-ffmpeg
mss

# Web / streaming
flask

# Model/helper dependencies
omegaconf
addict
einops
psutil
nvidia-ml-py
GPUtil
easydict

# 3D / reconstruction related
pycolmap
gsplat
trimesh
evo
