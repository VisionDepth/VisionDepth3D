Pillow
transformers
accelerate
numpy
matplotlib
opencv-python
tqdm
tensorflow
# Note: For PyTorch (torch), it’s recommended to let users install the CUDA-enabled version
# from the official PyTorch website (https://pytorch.org/get-started/locally/) based on their system.
